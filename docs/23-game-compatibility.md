# 23 — Game compatibility status

Every VR title tried on this rig (xrizer + Monado, bypassing SteamVR entirely — see
`docs/06-known-issues.md`), with its Steam AppID and a link to SteamDB. Status reflects the
last time each title was actually looked at with the headset on — per this project's core
rule (`CLAUDE.md`, "the single most important rule"), a title isn't "working" until a human
saw it, and several early sweep results below are marked accordingly as log-only, not
physically verified.

**This changed on 2026-08-12 (T163) and every "controllers are 3DoF" note below predates
it**: controller **position** tracking (constellation, via the headset's own cameras) now
works and was verified with the headset on. Rows written before that date describe
controllers rotating in place at a fixed offset, which was correct then and is not the
current state. Needs `WMR_CONSTELLATION_CONTROLLERS=1` and cameras running — see
`patches/monado/0024`–`0029` and `scripts/jack-in-wayland.sh`'s `ctrl` mode.

Still imperfect and measured as such: one controller can flip between two pose hypotheses
0.189 m apart, both good optical fits. Details and the reason the obvious fix (the IMU as a
prior) is not a drop-in are in `patches/monado/README.md`.

**A title only validates this if it draws the controllers.** Aircar — this rig's reference
title — does not, so none of the above is visible in it. Use one that renders hands.

## Working

| Game | AppID | SteamDB | Notes |
|---|---|---|---|
| International Space Station Tour VR | [797200](https://steamdb.info/app/797200/) | ✓ | Cleanest signal of the whole sweep (T073, T075) — zero warnings, FOCUSED, real 6DoF head tracking confirmed live. |
| International Space Station Tour VR — 2026-08-20/21 T243-night re-test, full 6dof+constellation added on top of the already-clean 6dof from T073/T075 | | | **The previously cleanest title in the whole sweep broke down this session — same "flying away" mechanism as Dead Herring VR and Chornobayivka VR, now 3/3.** Wearer (translated): "very unstable head tracking, I'm outside flying, hands sometimes appear in front of me, 30fps." Anchor age spiked to **2038ms and 2685ms** (vs. the 111-176ms range seen on the other two titles) on a `monado-service` process that had been running 8 minutes across two prior game sessions (Chornobayivka then this one) without a restart — raising a real hypothesis: session accumulation, not per-title cost. **Tested it properly**: full clean teardown + fresh `monado-service` restart, ISS Tour VR relaunched alone as an isolated control. Result: **partial improvement, not a fix** — pacing 4.04/3.70%/(cut short) on the fresh run vs. 8.96/5.81/3.48% on the accumulated one (roughly half the late-frame rate), but wearer's own verdict on the fresh run (translated): "improved a bit, yes, but still bad" — "I could start the game but my position arrives with very low frequency, hands sometimes anchored, sometimes fine." **Session accumulation is a real, measurable contributing factor (confirmed by the A/B) but not the root cause** — the problem reproduces from a genuinely fresh process too. Leading suspect, not yet confirmed: the binary is literally named `ISSTour-SteamVR8k.exe` — 8K content decode/render is plausibly the single heaviest per-title load measured this whole sweep, on top of which 6dof+constellation (never combined with THIS title before — T073/T075's clean verdict predates constellation entirely, T163) may simply exceed this CPU's budget (`monado-service` alone measured at **519% CPU**, over 5 of 12 threads, system load average 8.55 at the time). **Net effect on the night's broader finding**: the "flying away"/anchor-age-starvation mechanism is now confirmed on 3 different titles, engines, and content types, survives a from-scratch Monado restart, and gets measurably (not fully) better with one — pointing at real, additive CPU contention between SLAM+constellation and whatever the app itself demands, worse on session accumulation and worse still on heavy content, rather than a single simple bug with one fix. |
| Aliens Attack VR | [932190](https://steamdb.info/app/932190/) | ✓ | One `IVRChaperoneSetup_005` unknown-interface warning caused one session to cycle and exit, but recovered clean on a second attempt (T073, T076). |
| Aliens Attack VR — 2026-08-20/21 T243-night re-test, full 6dof+constellation | | | **19th title, same broken family, but the wearer's description here is the clearest mechanistic confirmation of the night.** Wearer: "very far from the spot, also about 30m off. Lots of delay to turn, but accumulated, weird. If I move a little, fine at first, then it takes that pulse and doesn't change it for a few seconds, then it stops and responds for a few seconds." This is a precise, in-the-wearer's-own-words description of dead-reckoning-on-a-stale-anchor: a rotation velocity gets captured once, then the extrapolator keeps applying that same stale velocity for several seconds (matching the anchor-age spikes measured all night, up to ~5s), until a fresh SLAM anchor lands and briefly corrects/catches up before going stale again. Worth quoting in full if this investigation gets picked up formally — it's the best plain-language description of the mechanism this whole sweep produced. |
| Cosmic Flow: A Relaxing VR Experience | [1267950](https://steamdb.info/app/1267950/) | ✓ | Confirmed working, real 3D (T073, T077). |
| Cosmic Flow — 2026-08-20/21 T243-night re-test, full 6dof+constellation, same accumulated `monado-service` (not a fresh restart) | | | **7th title tonight in the same broken family, and the worst instance yet.** Wearer (translated): "started fine, then immediately unexpected rotations and fps dropped to 0." Anchor age escalated hard within the same short log window: 745ms → 1706ms → **3339ms → 4949ms** — nearly 5 seconds stale, still zero `det(Q1Jl)==0` divergence events. `monado-service` at 433% CPU when it started. Fast, quick-triage verdict (per user direction to move through the fleet faster): **broken, same family as the rest tonight — do not treat as this title's own bug.** |
| VRSailing by BeTomorrow | [579050](https://steamdb.info/app/579050/) | ✓ | Confirmed working (T073, T079); also the title used to field-test the global recenter patch (`patches/xrizer/0001`) — was ~4m off its play-space center, recenters correctly with height preserved after the fix (T080). |
| VRSailing — 2026-08-20/21 T243-night re-test, full 6dof+constellation | | | **16th title tonight in the broken family.** Wearer: "30fps, somewhere random (all white), a controller sometimes jumps into view in front of me." Verdict: broken, same family. |
| SUPERHOT VR | [617830](https://steamdb.info/app/617830/) | ✓ | Trigger, grab, and hand tracking all confirmed working (T082). Menu button was dead on arrival (T067) — root-caused and fixed for the **left** controller via `patches/xrizer/0002` (T083-T084); right hand not expected to work, `Menu` is Left-only on the `oculus_touch` profile, matching real Oculus Touch hardware. |
| SUPERHOT VR — 2026-08-20/21 T243-night re-test, full 6dof+constellation | | | **14th title tonight in the broken family.** Wearer: "saw the starting cube where the game begins, was many meters higher up. Sound fine, I'm somewhere with no controllers in view." Verdict: broken, same family. |
| VRChat | [438100](https://steamdb.info/app/438100/) | ✓ | Confirmed working, reached `FOCUSED`, real gameplay, EasyAntiCheat loads clean under Proton (T082-session, same day). First attempt showed nothing — traced to an unrelated DP/panel dropout from heavy session churn that session, not a VRChat bug; a clean retest worked immediately. |
| VRChat — 2026-08-20/21 T243-night re-test, full 6dof+constellation | | | **15th title tonight, same fps ceiling — but the least severe symptom of the family.** Wearer: "30fps, the good part is the menu is correctly positioned in front." No flying/displacement reported this time, unlike most of the other 14. Still 30fps against the 90Hz target, so still counted broken by this sweep's bar, but worth flagging as the mildest case — possibly UI-anchored content (a menu, not a full 3D world) is less exposed to the SLAM-starvation extrapolation than free 6dof movement through space. |
| Propagation VR | [1363430](https://steamdb.info/app/1363430/) | ✓ | Best signal of the whole non-original sweep — "works just perfect", including exiting the game from inside via the controllers (menu/quit works out of the box, no patch needed unlike SUPERHOT). First launch had controllers powered off (Monado has no controller hot-add — confirmed `<none>`/`<none>` in the log), retested clean after a full jack-in-wayland.sh restart with controllers on beforehand. |
| Propagation VR — 2026-08-20/21 T243-night re-test, full 6dof+constellation | | | **17th title tonight in the broken family.** Wearer: "40fps, I see the map like from 30 meters below." Note the fps this time (40) doesn't land on the exact 90/2 or 90/3 pattern seen elsewhere — consistent with the earlier "variable, not a fixed cap" read from Interkosmos's follow-up detail. Verdict: broken, same family. |
| Aircar | [1073390](https://steamdb.info/app/1073390/) | ✓ | **RECOMMENDED — the reference title for this rig (re-verified 2026-08-12, T161).** User verdict: "100% funcional con sticks VR o Xbox 360 por igual, buen performance, sonido en casco". Works with either input device, interchangeably. Measured in the same session: **89-90 fps** at `4320x2160@90` with the default 140% render scale (3024x3024 per eye), and **0.13% late frames** (0.12/s) once warmed up. **Warm-up caveat that applies to every Proton title, not just this one**: the first measurement of the same session read 3.44% late frames (3.10/s) with the render scale *unchanged* -- that window caught DXVK shader compilation and asset streaming, not steady state. Its `DXVK_state_cache` directory is empty, so first-run compilation is heavy. **Do not judge a Proton title's pacing in its first minute.** Audio routes to the headset and PipeWire's stream-restore remembers it per-application across Monado restarts. VR-controller stick drift needs `WMR_STICK_DEADZONE=0.15` (patch 0008) -- WMR controllers carry no stick centre calibration at all; without it the drift is plainly felt and was initially, wrongly, blamed on the Xbox pad. Earlier verdict: "first game I'd call 99%" — real VR, full controller input, excellent GPU utilization. First launch fell to 2D-only via the localconfig launch-options trap (see the note below this table). Two session-level issues observed, neither the game's fault: a pronounced CCW **roll drift** on the long-uptime 6DoF/SLAM session (horizon re-tilts right after every recenter — recenter is yaw-only by design), and on the fresh-SLAM session a wrong start position ("outside the vehicle") from the origin anchoring where the headset sat. See T106 for the SLAM-divergence follow-up. |
| Aircar — 2026-08-20/21 T243-night re-test, constellation off per its own title profile — **inconclusive, NOT counted against it** | | | Game reached a running process (confirmed live, PID present) with both controllers paired/online/registered by Monado, but the start-menu "press A" prompt never responded — **no Xbox pad was connected to the system at all** (zero devices in `/proc/bus/input/devices`, no `/dev/input/js*`), so this is very likely an input-device mismatch (trying a physical pad button that isn't there, or the VR-wand-as-gamepad binding not covering that specific prompt) rather than a tracking regression — this session never touched Aircar's own working config beyond the constellation-off profile it already uses. **Kept as this rig's reference title regardless of tonight's result** (user's own call) — not re-added to the broken-family list, needs a real retry with either a genuine Xbox pad or a confirmed VR-wand binding before any verdict changes. |
| Aircar — 2026-08-23 (T245), **docs/67 S1 exam run #1**, 6dof head, constellation OFF, Xbox pad, machine pinned at 70 % GPU, launched through `vr-launcher.py` hands-free | | | **Render half of the bar MET, head-SLAM half NOT (dim light).** App fps by `Delivered frame` (new `scripts/app-fps.sh`): 89.5/79.1/85.2 while loading, then **88.9-90.05 in four 3×20 s windows**; compositor **0 late frames in 6×30 s**; GPU 58-82 % at 174 W with the 70 % power cap ACTIVE the whole time (clocks ~1.8 GHz) and fps still at target — the cap is no longer free like T209, it is just still enough. Companion reconnect at 00:14 (3.3 s dead) left zero holes (0093/0094). Wearer (translated): *"appeared inside, all fine"*; provoking it: *"walked a few metres back and forth, that accumulates error and I ended ~5 m outside the ship; A recentres"*. The raw SLAM pose says why: **three accelerating runaways in the first 75 s seated** (two cut by the 10 m/s auto-reset, the third peaked at 3.8 m/s, never reset, and parked the raw pose at 41 m), both walks (00:12, 00:15) show as tens of metres of raw excursion, and seated flying drifted ~4 m in 12 min (0.33 m/min, T163's number). Basalt throughput was normal throughout (300 frames/10 s, 40 ms) — **not CPU starvation: it is the dim room** (docs/56 class); from t+340 s SLAM ran at 26 fps / ~100 ms latency (optflow threads 100/80/80/80 %), the machine at 37 %. Resting afterwards: stable 5.5 min, then a reset storm (#3→#32) from 00:30:12 — the wearer handled the headset at ~00:30 (his timing), so resting is not a control here (docs/59 rule). Teardown: game stopped clean, `down` hung past 10 s → SIGKILL, no core (docs/67 C1 family). **Certification OPEN — run #2 (S3) in normal light, ≥ 30 min worn; light A/B deferred by the user tonight.** |
| Orientation drift RESOLVED for resting controllers — 2026-08-13 (T165) | | | **The row below is superseded for controllers at rest.** The 20-70 °/min was not gyro drift at all: ~30 s after a G2 controller stops moving its firmware keeps the packet stream alive but **zeroes the IMU fields**, and the calibration pipeline turned those zeros into each device's factory bias vector — a fake constant rotation, R²=1.000, rate different per device and per session. Three patches (`0033`–`0035`: estimator smoothing, prediction cap, zeroed-packet detection) take a resting controller from 31–72 °/min to **+0.00 °/min, residual 0.00°**, measured on the fixed protocol (`scripts/drift-measure.py`). Still open: in-hand drift during play (unmeasured on the new binary), and the head's own ~65 °/min bias now auto-corrected at rest. Full chain in `docs/pruebas.jsonl` T165. |
| Orientation drift — 2026-08-12 (T164), applies to EVERY title | | | **The roll / yaw drift this file attributes to long 6DoF SLAM sessions is not a SLAM problem.** It reproduces in **3dof**, where Basalt is not running at all. Root cause: `m_imu_3dof` has a gyro-bias estimator that nothing ever calls — `gyro_biasing()` returns unless a debug-GUI checkbox is set. Measured with both controllers untouched on a desk, two independent windows within 1%: **left 72 °/min, right 20 °/min**, and the same rates in a different resting orientation, so it is plain gyro bias and not accelerometer misalignment. Gravity correction pulls pitch and roll back but has **no reference for yaw**, which is why yaw is what the wearer notices. `patches/monado/0031` fires the estimator automatically when the device is still: inter-controller drift **18.8 → 8.9 °/min**, a 53% reduction, not a cure. Any per-title note below describing drift predates this. |
| Aircar — recentering (T163) | | | **The A button on the Xbox pad recenters the view in-game**, found by the user 2026-08-12. That matters more than it sounds: SLAM position drift on this rig is 0.37 m/min with the headset static (measured, 0.61 m over 100 s), so on a long session the wearer ends up "1 to 3 metres outside the ship" — and this makes that recoverable in place, without restarting the runtime or relying on xrizer's own hold-menu recenter (`patches/xrizer/0001`), which lives in the legacy input path and may never be reached by a title that ships its own action manifest. **Anchor the origin properly at startup too**: the SLAM origin lands wherever the headset physically is when the service starts, so start it with the headset already worn and in the play position, not sitting on the desk. |
| Aircar — 2026-08-18 stick-drift VERDICT (T216) | | | **Stick input drift RESOLVED, wearer-verified with the game's own instrument**: Aircar's in-cockpit dashboard displays the three stick axes numerically — a live diagnostic — and with `WMR_STICK_DEADZONE=0.15` (the launcher default since 2026-08-18) the wearer reports "el shift se fue del todo, puedo dejar todos los ejes en un valor fijo y volar en línea recta como se manda". Deadzone alone suffices; `WMR_STICK_AUTOCENTER` (0070) stays available as optional refinement (would let the deadzone shrink toward 0.05), not a need. Same session: pacing 6.89/8.19% late (median = one full slot, the known CPU-bound signature, historical band — the two anti-ghost layers 0074/0076 cost nothing measurable). |
| Aircar — 2026-08-12 late session (T163) | | | **Performance in 6dof is the finding, and it is the SLAM's cost, not a regression from the controller work.** Measured with the frame-pacing instrumentation: 6dof shows **7.2–11.6% late frames** against **2.8%** for the same title in 3dof, and the user reads it directly as **50–70 fps in 6dof vs 80–90 in 3dof**. Mechanism found with `ps -L`: Basalt runs single-threaded and pins **one whole core at 99.4%** (of six physical, on a Ryzen 5 5600X) while the machine sits at 26% overall. **Giving it more threads is WORSE, measured, not assumed**: `num-threads=3` produced four monado threads at 94–99% each and 10.8–11.6% late frames across three consecutive windows. `num-threads=1` stays the default, now as a documented variable rather than a literal (`SLAM_THREADS`). **Constellation controller tracking is NOT the cost** — with it off, the same config measured 3.44% and 7.22% in back-to-back windows, i.e. the run-to-run variance is wider than the effect. Three windows minimum per data point. **The wearer's "the camera snaps back to a previous position about once a second, only when turning" is the dropped frames**, not a tracking bug: a frame that misses its slot makes the compositor re-show the previous one, which is invisible with the head still and obvious mid-turn. |
| Google Earth VR — 2026-08-18 full-6dof re-test | | | On the ghost-saga stack (0055+0071+0074/0076, spread=60, deadzone 0.15) and the NEW xrizer (0004 legacy+menu build, its first unplanned field test — no regressions): wearer "casi utilizable. Controles un poco raros, pero anda bastante bien" — a big jump from the 2026-08-09 6dof "unusable" verdict (that was the diverging-SLAM era). The odd controls match the known hands-position gap (~95% parked, T215) — this title grabs the world with the hands, so it feels it directly; re-test after the seeded-recovery (0077) A/B. |
| Google Earth VR — 2026-08-20/21 T243-night re-test, both controllers freshly paired, full 6dof+constellation | | | **Positional hand tracking confirmed live for the first time on this title**: 162 `get_tracked_pose`/`position_tracked` log lines during the session, constellation genuinely solving throughout (not just enabled-and-idle) — the ~95% parked-hands gap this row's predecessor described did not reproduce here, though the wearer's own verdict on hand feel specifically wasn't captured separately from the overall "usable" call. **Real, reproducible discrepancy found, NOT root-caused**: compositor-side pacing (`frame-pacing.sh`, 3 windows, GPU pinned to 100%/210W) measured **0.07-0.15% late frames at 90Hz — clean**, but Steam's own overlay read a steady **45 fps across two independent launches** (quiet-mode and a full-relaunch in verbose/`VR_PACING=1` mode, same number both times, not warm-up noise). 45 is exactly half of 90 — the leading, **unconfirmed** hypothesis is the app rendering at half rate while Monado's compositor still presents at the full 90Hz cadence (reprojecting/repeating a frame the compositor-side pacing metric can't distinguish from a freshly-rendered one). Not chased further this session — a next step would be correlating `xrWaitFrame`/app-frame-submission timestamps in Monado's debug log against Steam's counter, not just compositor present timing. Also noted, separate and likely unrelated axis: **~2 minutes to a usable scene**, consistent with online map-tile streaming, not GPU/tracking bound (GPU sat at 210W). **Wearer's verdict (translated from Spanish)**: "the left controller has 6dof and I could handle the basics, works pretty solid. Missing the [right] one and it'd be complete. Sometimes felt like I was somewhere below the map, not sure if that's on our end or the app's." So the 162 logged solves were real and the **left** controller genuinely tracked by position — but the **right** controller, despite registering correctly as a device this session (confirmed in `left:`/`right:` log lines), did not track positionally for the wearer, an asymmetry not explained by anything measured this session (left is also the controller freshly re-paired via T243's own pairing3.pcapng capture hours earlier — unclear if related or coincidental). The occasional "below the map" spawn feeling is left explicitly unattributed (could be this project's flat-floor origin assumption, T146, or the app's own level geometry) — not chased further. **FINAL VERDICT for this pass**: usable, real progress (first-ever confirmed live positional hand tracking on this title), but not clean — right-hand position tracking, the Steam-vs-compositor fps gap, and the map-boot delay are three distinct open threads, not one. |
| Google Earth VR | [348250](https://steamdb.info/app/348250/) | ✓ | **Works perfectly on a 3dof session** — user (translated): "works perfectly… all its controls work perfectly, you can see it on the 3D model inside" (the in-game controller model confirms every input works). On the same night's *diverging* 6DoF/SLAM session it was unusable (head jitter + view flying away — Basalt numerical divergence, `det(Q1Jl)==0` cascades in the log; light on didn't help, recenter didn't either). Controller *position* missing is the known project-wide 3DoF-controllers limitation, not a game issue. First launch attempt also hit the localconfig trap ("OpenVR failed to initialize / InterfaceNotFound" was the visible symptom). |
| Dead Herring VR | [1498490](https://steamdb.info/app/1498490/) | ✓ | Real VR image and gameplay reached; user was mispositioned (same diverging-SLAM session as Google Earth — needs a clean-session retest for a final verdict, but the game itself renders and runs). Curiosity: has a 2D debug mode showing a map on the desktop window in 3D. |
| Hellblade: Senua's Sacrifice VR Edition | [747350](https://steamdb.info/app/747350/) | ✓ | **New title, first attempt 2026-08-21 (T243-night), not vanilla Hellblade — a real VR mod/edition, user has played it before on WMR and Quest 2.** First real look (translated): "very promising, steady 45fps, good head guidance." Same 45fps ceiling as several other titles the same night — worth re-checking against `docs/06`'s new `pop_pose()` segfault finding and the general SLAM-load pattern once that's understood, but the wearer's own read here is notably more positive than most of the night's other titles ("promising" vs "broken"). Downloaded live during the session (24.7GB), first real test, deserves a proper full pass later rather than a one-line verdict. |
| Wolfenstein: Cyberpilot | [1056970](https://steamdb.info/app/1056970/) | ✓ | **New title, first attempt 2026-08-21 (T243-night), on a fresh Monado restart after a USB2-branch cable replug.** Wearer (translated): "very promising! Works, tracks my head in 6dof. I left the field of view and now it alerts me about that. Seems a bit slow, seeing 30fps. But good anyway!" Real native Bethesda VR title (id Tech, `Cyberpilot_x64vk.exe`), genuine 6dof head tracking confirmed, plus a working out-of-tracking-volume warning — the clearest positive result of the whole late-night follow-up round. Same 30fps ceiling seen elsewhere the same night, but the wearer's own verdict here is unambiguously good, not just "usable." **2026-08-25 reinstall on the NVMe/NTFS library (`/mnt/videos/SteamLibrary`): two distinct bugs found and fixed via `bench-launcher.py cyberpilot --tracking 6dof`.** (1) `compatdata/1056970`'s Proton prefix hit docs/70's exact `dosdevices/c:` `Errno 22` (ntfs-3g refuses the symlink) — process died in ~2s, no wine log captured (`launch_steam()` uses DEVNULL), only visible via Steam's own `console-linux.txt`. Fixed with docs/70's recipe: `compatdata/1056970` relocated to `~/proton-prefixes-external/1056970` via symlink. (2) Even after the prefix fix, `--tracking 6dof` alone left constellation off (`left: <none> right: <none>`) and the title's OpenXR session opened and closed within a single log line (`BEGIN_SESSION`/`END_SESSION` adjacent, no frames between) — `bench-launcher.py` gained a `--controllers` flag (`WMR_CAMERAS=1 WMR_CONSTELLATION_CONTROLLERS=1` on top of any `--tracking` mode, since the enum alone can't express SLAM+constellation together) which fixed controller registration (`left/right: HP Reverb G2 ... Controller`) and got the prefix loading for real (17 tracked processes, 73s+ alive) — but the SAME instant `BEGIN_SESSION`→`END_SESSION` pattern still happened, immediately followed by ~2000+ frames of Monado idling with no client connected until our own script's teardown. Leading read: this is an idTech intro-video/menu-navigation gap, not a tracking config bug — `bench-launcher.py`'s unattended ~70s window (10s wait + 3×20s sampling) is plausibly too short for this title's own boot sequence (splash videos, EULA, main menu) to reach a second, real, sustained XR session; unlike Aircar/Quake2 this title has never been confirmed to work unattended, only with a human navigating past menus. **Not yet resolved**: needs a human to wear the headset and push past the intro/menu manually, then a fresh performance/stability measurement pass. Also found: `bench-launcher.py`'s teardown (`game-stop.py stop`) didn't kill the process on the first automated run (had to be stopped manually afterward) — not chased, flagged for a future session. **RESOLVED same day, with a human wearing the headset**: the instant `BEGIN_SESSION`→`END_SESSION` was a real, harmless artifact — a SECOND `BEGIN_SESSION` follows within ~1s (an xrizer/engine capability probe, not a crash) and opens the real session; this only fires once the headset is actually WORN (proximity/wear sensor), which is why every unattended `bench-launcher.py` run saw only the probe and never the real session — not a menu-navigation gap after all. Ran a full manual A/B (`jack-in-wayland.sh` directly, `WMR_CAMERAS=1 WMR_CONSTELLATION_CONTROLLERS=1 6dof`, human wearing the headset each time) to finally answer the original question this reinstall was for — `vr-prewarm.sh` cache (genuinely cache-cold, `echo 3 > drop_caches` first) vs ram mode: **ram wins on load** — first real frame at ~22s vs cache's ~28s, and ram's climb to steady 90fps was clean and fast (30→89→90fps within 2 more seconds) where cache's climb was noisy and dipped mid-ramp (16.5→52→60→55→**36**→59fps, never cleanly stabilized in the sampled window). Once both arms reached steady, unfocused, real-gameplay state, fps converged to the same ~62-66 range regardless of prewarm method — confirms prewarm only buys the load transition, not steady-state, matching the T246 Aircar precedent. **Second, unrelated, major finding**: window focus state matters enormously and reproduced identically across BOTH arms — fullscreen+focused locks to a hard 30.00 fps ceiling (`Fake pacer fell behind` spam in Monado's log), windowed+focused is noisy 60-89fps, any unfocused/background state (windowed or fullscreen) runs clean ~90fps when idle. Promoted to a general project testing rule (measure windowed vs fullscreen per title, default windowed). Tradeoff found: unfocused seems to affect audio (not chased). Both arms measured 40-45% late frames throughout (`pa_mark_gpu_done`) and anchor age 133-171ms during load — the first time this project has run head SLAM 6dof + controller constellation together, and pacing clearly costs real headroom versus either alone. **Wearer's verdict, extended real play (translated)**: controllers "aren't perfect in the 6dof sense, they jump" (the known docs/58 controller-position residual, reconfirmed); moving the head "the refresh is still annoying" (matches the measured 40-45% late-frame rate — judder from missed pacing deadlines shows up specifically during motion, invisible at rest); "if I stay still, the headset's 6dof is almost perfect" (matches the passive in-session drift read: ~31mm position spread / ~1.3° orientation drift over 21.5s stationary); "the joysticks let me manage okay overall" — and after tuning in-game controls and turning off render supersampling, the first (dog) mission became genuinely playable. Net: this title is real, playable, and now unblocked for future sessions (prefix fixed, `--controllers` flag exists) — the residuals are the project's already-known ones (controller jitter, SLAM+constellation pacing cost), not anything new to this title. **Follow-up same day, mechanism found (memory: `feedback_windowed_default_fullscreen_ab`)**: the earlier "focus" framing was imprecise — the real driver is window VISIBILITY, not keyboard focus. The desktop monitor runs 59.96 Hz (`xrandr`); a windowed, non-fullscreen, non-"focused" companion window still measured a clean 60.0-60.4 fps lock (matches the monitor almost exactly) because mutter has to composite any VISIBLE window against the desktop's own vsync. `xdotool windowminimize` on that exact window, same game state, same instant, snapped it straight to 90.00-90.20 fps — immediate, fully reproducible. **Practical rule: minimize the game window, don't just alt-tab away from it, for a real 90fps VR session on this rig.** Believed Linux/mutter-specific, not expected on Windows. |
| Sniper Elite VR | [752480](https://steamdb.info/app/752480/) | ✓ | **New title, first attempt 2026-08-21 (T243-night).** Wearer (translated): "45fps at times, would probably work if we solve what's blocking us. I see the world way up high." Same fps-ceiling and vertical-offset pattern as most of the night's broken family — no new distinct mechanism, but the wearer's framing ("would work if...") reads as a real, playable-once-fixed title rather than fundamentally broken. |
| Vertical Shift | [1807480](https://steamdb.info/app/1807480/) | ✓ | **New title, first attempt 2026-08-21 (T243-night), installed and tested live.** Wearer (translated): "30fps, I'm far from the map." Same fps-ceiling and mispositioning pattern as the rest of the night's broken family — no new distinct mechanism. |
| Transmissions: Element 120 | [365300](https://steamdb.info/app/365300/) | ✓ | **New title, first attempt 2026-08-21 (T243-night), mixed and inconclusive.** First real attempt: panel never lit ("didn't turn on"), traced live to the known `docs/06` companion USB2 drop — a continuous storm of `os_hid_read returned -1` on the panel's control channel, confirmed by a 2/5 census right after (companion + audio missing). Physical cable replug + fresh Monado restart fixed the census (5/5), but two retry launches afterward both exited in ~2-3s with no `client_connected` at all — a different, unexplained failure. Not root-caused further this session; retest queued. |
| Tank Mechanic Simulator VR | [1463010](https://steamdb.info/app/1463010/) | ✓ | Confirmed working, 2026-08-09 — user (translated): "perfect! 3dof on the controllers. Excellent image." Shows a generic "does not appear to support headset" warning on first launch (same harmless whitelist-check pattern as Dark Room VR) — click through it, doesn't block anything real. |
| Aperture Hand Lab | [868020](https://steamdb.info/app/868020/) | ✓ | **New title, first attempt 2026-08-21 (T243-night).** Valve's free hand-tracking tech demo (binary `Knux.exe`, installdir `Knux` — the real name only shows in the Steam manifest). Real session reached (`BEGIN_SESSION`/`END_SESSION` cycles, one client stayed connected for a real stretch), wearer briefly saw image in the headset, then hit an in-game "please relocate to center in 2D screen" fallback message and dropped to 2D — traced live to `docs/06`'s already-known companion USB2 drop (`os_hid_read` errors on the companion HID device, confirmed re-enumerated with a fresh device number right after). Not a new bug, the known intermittent USB2 branch reset catching a title mid-session for the first time this clearly. Also **30fps** — a third distinct submultiple this sweep (30, 40, 45 all seen on different titles), feeding the wearer's own fps-ceiling hypothesis below. |
| Tank Mechanic Simulator VR — 2026-08-20/21 T243-night re-test, full 6dof+constellation | | | **13th title tonight in the broken family.** Wearer: "30fps. At least there's a menu in front of me. Hands sometimes appear in view and I can even move them a bit — they don't point in the direction they should." So constellation position is partially there but orientation is wrong on top of it, a compounded symptom vs. the pure fps/position issue seen elsewhere. Verdict: broken, same family. |
| SafeZoneVR | [1701090](https://steamdb.info/app/1701090/) | ✓ | Image/gameplay confirmed working (2026-08-09), but head-tracking drifted left during both attempts, including a fresh-SLAM restart — **not the usual `det(Q1Jl)==0` numerical-divergence signature** (zero such warnings in the log either time, unlike Aircar/Google Earth VR), so likely plain SLAM/VIO drift rather than the known divergence bug. The global recenter shortcut (hold menu 3s) never fired during either SafeZoneVR attempt (`xrizer.txt` shows zero "menu button held" lines that session, which itself exited on its own after only ~61s, clean `STOPPING→IDLE→EXITING`, not a crash). **Confirmed NOT a general recenter regression**: immediately re-tested on VRSailing (same session, same xrizer build) and it fired twice, ~3.0s hold each time, exactly as designed. So the SafeZoneVR case is either that the ~61s session never gave a full 3s of continuous hold a real chance, or something specific to that game/session — not root-caused further this session, redo with a longer SafeZoneVR run if it recurs. |
| SafeZoneVR — 2026-08-20/21 T243-night re-test, full 6dof+constellation, GPU 210W | | | **A different symptom than the 2026-08-09 left-drift row above, not a repeat of it.** Wearer (translated): "30fps, I appeared about 10 meters above where I should be" — spawned roughly 10 m too high, not the earlier left-drift. **No `det(Q1Jl)==0` divergence signature this session either** (still zero, same as before) — so two separate SafeZoneVR sessions now both rule out the known numerical-divergence bug while showing two different position-anchoring symptoms, meaning whatever is wrong here is neither of the already-named causes. **Pacing measured this time, and it's real, not just a counter mismatch**: window 1 (20s) showed **3.67% late frames, median exactly one full 11.11ms slot — genuinely missing slots, not jitter** (contrast Google Earth VR's clean 0.07-0.15% same night). Window 2 was cut short by closing the title before it finished, not a measurement failure. Still, 3.67% late alone doesn't account for a Steam-reported 30fps against a 90Hz target — same open pattern as Google Earth VR's 45fps/clean-pacing mismatch, now seen on a second title, which makes it look more like a systemic Steam-overlay-vs-compositor discrepancy than a per-title fluke. **Not root-caused**: the 10m spawn offset, the real (if partial) pacing degradation, and the fps-counter gap are three separate open threads. Worth checking next time: `SLAM_AUTO_RESET`'s 10 m/s divergence-velocity threshold (logged as active this session) is a suspicious near-miss on "10" against the wearer's "10 metros" — almost certainly coincidental (different units, velocity vs. position) but cheap to rule out explicitly before trusting that it's unrelated. |
| Dead Herring VR — 2026-08-20/21 T243-night, three-way tracking-mode A/B (6dof+constellation / 6dof-only / 3dof-only) | | | Wearer initially (translated): "45fps too. Flying away at many meters. Left controller visible sometimes with 6dof... looks like we broke something huh" — genuinely worth checking rather than assuming a per-title issue, since the exact **45fps (= 90/2)** had also just been measured on Google Earth VR the same night. **Ran the controlled A/B instead of guessing.** (1) 6dof+constellation: pacing 2.93/1.26/1.78% late (real drops, not jitter), CPU genuinely saturated (two monado-service threads >80%, three `bslt-optflow` threads 48-66%, load avg 6.90/12), SLAM "anchor age" climbing live in the log (128→141→155→157ms) right before the wearer's "flying away" report — a real, distinct-from-usual mechanism: SLAM falling behind real-time under load, not the known `det(Q1Jl)==0` numerical-divergence bug (stayed at zero occurrences all three runs). (2) 6dof only, constellation OFF: **still 45fps** — pacing genuinely improved (0.63/0.67/0.48%, roughly 2-4x fewer drops than with constellation on) but the fps ceiling didn't move at all, so constellation costs real compositor pacing but is NOT the 45fps cause. (3) 3dof only, no SLAM at all: **still 45fps**, wearer (translated): "now I'm where I should be [without 6dof]" (position correct again, as expected with rotation-only tracking) — pacing near-perfect (0.11%, 0.22%; a 6.89%-late third window is very likely a shutdown transient from killing the process mid-measurement, not steady state). **Conclusion: the 45fps ceiling survives all three tracking configurations, including zero SLAM/constellation load — it is NOT caused by this project's tracking stack.** Combined with Google Earth VR landing on the exact same 45fps and SafeZoneVR on exactly 30fps (=90/3) the same night, this reads as something in the app/DXVK/Proton present path (or how Monado's compositor negotiates frame rate with these specific OpenXR clients), independent of tracking mode — a real, reproducible, **not yet root-caused** finding, not a regression from the pairing/USB work earlier tonight (unrelated subsystem). The SLAM-starvation "flying away" mechanism (anchor age growing under CPU load) is a separate, second finding from the same session, real but only observed under the heaviest (6dof+constellation) condition. **Reproduced same night on a second title (Welcome to Chornobayivka VR, see its own row): anchor age 111-176ms under similar CPU load (monado-service + `bslt-optflow` threads 43-60%), zero `det(Q1Jl)==0` events, wearer again reported flying away — this is now a 2/2 reproducible phenomenon, not a one-off.** |
| Interkosmos | [579110](https://steamdb.info/app/579110/) | ✓ | Confirmed working 2026-08-12 (T161) — 3DoF hands, video perfect. Sound not verified (none reached the user in the opening section; may appear further in). |
| Interkosmos — 2026-08-20/21 T243-night re-test, full 6dof+constellation, **fresh clean `monado-service` restart** | | | **8th title tonight in the broken family — and importantly, the 2nd tested on a genuinely fresh Monado process (after ISS Tour's control).** Wearer (translated): "unexpected rotations, hands fine at times, same [issue]." Anchor age still spiked to 921/2341ms even on a clean start, `monado-service` at 374% CPU. **This rules out session accumulation as the sole explanation a second time** — a fresh process still hits the same starvation pattern, just perhaps somewhat later/less severely than a long-accumulated one. Quick verdict: broken, same family. **Wearer's follow-up detail, worth keeping**: "30fps, but seems related to... sometimes throws 40 in the same spot, like some pre-draw calculation is driving it crazy" — fps *varying at a fixed location*, not a stable fixed cap, which fits variable per-frame SLAM/optflow compute cost stealing cycles unevenly better than a fixed engine-side frame-rate cap. **Correction (caught by the user next title over — this quote was originally, wrongly, attributed to Emergence below; it belongs here)**: "sometimes it grabs some 6dof headset movement and extrapolates it to infinity... cuts every 10 seconds and a spin starts, all like that." |
| Emergence | [1337820](https://steamdb.info/app/1337820/) | ✓ | Confirmed working 2026-08-12 (T161) — plays well with the VR sticks. **0.41% late frames** once warm. Audio needed routing to the headset by hand (see the audio note below the tables). |
| Emergence — 2026-08-20/21 T243-night re-test, full 6dof+constellation, same (not-freshly-restarted) `monado-service` | | | **Wearer verdict pending — the quote originally recorded here was actually about Interkosmos (see that row), caught and corrected by the user.** What IS genuinely Emergence's own data (sampled from its own session, timing confirmed): anchor age log mostly calm (111-118ms) with real spikes up to **4904.7ms** in the same window, process exited on its own before a manual close was needed — consistent with the broken family but not physically confirmed by the wearer for this specific title. **Re-tested properly right after (12th title tonight): wearer confirms "rotations, 45fps" — broken, same family, now physically verified.** |
| Blast the Past | [943170](https://steamdb.info/app/943170/) | ✓ | Confirmed working 2026-08-12 (T161) — hands in 3DoF and responding. **The cleanest pacing result measured so far: ZERO late frames in 30 s** (0.00–0.09%). |
| Blast the Past — 2026-08-20/21 T243-night re-test, full 6dof+constellation, **NOT a clean control** | | | **Methodology caveat first**: this run reused the `monado-service` process left over from the ISS Tour VR control test (session accumulation, not a fresh restart) — the light-vs-heavy-content comparison this row was meant to give is confounded, noted here rather than hidden. Still: even this "cleanest pacing ever measured" title now shows the same pattern — **45fps** (=90/2, same ratio as Google Earth VR/Dead Herring VR), wearer (translated): "smooth rotations nobody asked for, like base-station docking inertia, seeing the city from above, controller sometimes pops up in front of me as it should — broken." `monado-service` measured at 453% CPU during this run, essentially the same order of magnitude as the 519% seen on the much heavier 8K ISS Tour session — **suggestive that the tracking stack's own CPU footprint (SLAM+constellation+verbose logging) dominates over app content weight**, not yet confirmed with a real clean-session A/B for a light title specifically. Pacing 1.67/(mid)/1.81% — worse than this title's historical 0.00-0.09% (that baseline predates 6dof/constellation entirely, T161 was hands-in-3DoF). **4th title tonight in the same broken state** (Google Earth VR, SafeZoneVR, Dead Herring VR, Chornobayivka VR, ISS Tour VR, now this one) — strong enough pattern that a dedicated clean-session light-title control is worth doing before adding more titles to the pile, rather than assuming this row settles it. |
| Blast the Past — 2026-08-17 full-6dof re-test (T204) | | | Re-tested on the post-clock-fix stack (0055, threads=4, machine pinned, constellation ON): head 6dof good, displacement very good, some turn delay remaining (SLAM correction snaps + dropped-frame re-shows, T163's mechanism). **Pacing 4.0-7.2% late at 3024x3024 under FULL 6dof+constellation — better than T163's 7.2-11.6% 6dof baseline; the 2026-08-12 0.0% row above was the light-load/3dof-hands era, not comparable.** Also the stage for the first GPU power-vs-pacing A/B: 147W (70%) vs 210W indistinguishable — the late frames are CPU/scheduling (median exactly one 11.11ms slot), not watts. **New inventory dimension found here: title POSTURE matters for hands** — this is an aim-low title, so the controllers sit under the camera FOV cone and park at their last solve with IMU-only orientation, while hands-up content (the 360 viewer, same hour, same stack) tracked 6dof clean. Constellation solves confirmed flowing in the log while the wearer saw parked hands. |
| Audio Factory | [722590](https://steamdb.info/app/722590/) | ✓ | Confirmed working 2026-08-12 (T161). Two observations that are **not this title's fault**: the user sat too high (Monado assumes a flat 1.6 m floor below the headset at startup, T146, project-wide) and one controller was 3DoF-only (the paused controller position tracking, `docs/03`). |
| Audio Factory — 2026-08-20/21 T243-night re-test, full 6dof+constellation | | | **10th title tonight in the broken family.** Wearer: "same, 30fps." Fast triage, no deeper instrumentation — verdict: broken, same family. |
| VersaillesVR \| The Palace is yours | [1098190](https://steamdb.info/app/1098190/) | ✓ | Confirmed working 2026-08-12 (T161) — sound in the headset, both sticks visible and registering movement. **0.37% late frames** once warm. Started at ~2 fps and settled on its own: the most extreme warm-up case measured (see the warm-up note below). |
| VersaillesVR — 2026-08-20/21 T243-night re-test, full 6dof+constellation | | | **11th title tonight in the broken family.** Wearer (translated): "same, weird rotations." Fast triage, no deeper instrumentation — verdict: broken, same family. |
| Steam 360 Video Player | [613220](https://steamdb.info/app/613220/) | ✓ | Reaches VR with controllers visible, but renders black **for lack of content** — the project's own test media is down to a single equirect JPG. Structurally fine; verification incomplete rather than failed (2026-08-12, T161). |
| Steam 360 Video Player — 2026-08-20/21 T243-night re-test, full 6dof+constellation, fresh clean restart | | | **18th and last title of tonight's sweep.** Wearer: "35fps. If the headset doesn't fly away, it has nice tracking." Notably **no runaway-extrapolation event this time** despite full 6dof+constellation and the same fps-ceiling pattern (35, another non-exact fraction) — supports the earlier read that the flying-away mechanism is intermittent/load-triggered, not guaranteed every session. Content gap from T161 unchanged (still no real test video, same as before). |

> **Correction to a conclusion in `CLAUDE.md` (2026-08-12, T161).** That file records Funhouse,
> InCell VR and InMind VR as "each fail for their own unrelated reason … **not a shared bug**".
> That does not hold. InCell and InMind share one cause exactly (the missing Oculus
> `OVRPlugin`), and Funhouse was separately unable to reach the runtime at all because its
> launch options were incomplete. Three different crash signatures, two shared causes, and
> none of the three was ever observed under a working configuration.

| Reelease | [3609930](https://steamdb.info/app/3609930/) | ✓ | **Not in this doc before tonight — found installed and untested, 2026-08-20/21 T243-night.** Purpose/genre not established this session (no time to explore beyond the tracking check). Reaches VR: wearer "no fps counter visible, headset flying, controllers sometimes visible with a laser pointer" — real content and controller input reached, but same broken-family symptom as the rest of tonight's sweep (20th title). Revisit once the SLAM-load issue has a fix, and note what this title actually is. |

## Runs in 2D but never enters VR — opens an XR session and abandons it

A distinct failure class, named 2026-08-12 (T161), previously lumped in with "doesn't work".
These titles **do** talk to OpenVR: they create a session and then drop it inside the same
millisecond, while the process stays alive and plays perfectly flat on the desktop. Identical
signature in all three:

```
OpenXR session state changed: SYNCHRONIZED -> STOPPING -> IDLE -> EXITING
```

That is neither a crash nor a plumbing gap. The likely cause is a missing per-title launch
flag; **`steam -applaunch <id> <args>` does NOT forward arguments** (verified — the game's
cmdline ends at the .exe), so it has to go in the launch options as `%command% -vr` or
whatever the title expects, which needs Steam closed. Untested as a batch.

| Game | AppID | SteamDB | Notes |
|---|---|---|---|
| Dagon | [1481400](https://steamdb.info/app/1481400/) | ✓ | Runs perfectly in 2D; the user recalls having played it in VR before and could find no in-game way to switch. |
| Back to Dinosaur Island | [412940](https://steamdb.info/app/412940/) | ✓ | Runs fine in 2D, never takes the headset. CryEngine, like its sequel — but this one does not crash. |
| Amoreon NightClub | [701100](https://steamdb.info/app/701100/) | ✓ | 2D only, appears to run fine. |
| BlazeRush | [302710](https://steamdb.info/app/302710/) | ✓ | First-ever launch attempt 2026-08-13 (T167): the **native Linux build exits in under a second** (its install dir carries both the native `blazerush` binary and a Windows `game.exe`). Its VR support historically belongs to the Windows build — testing it in VR means forcing Proton for this title from the Steam UI, not yet done. Not a failure of this stack; the native binary just isn't the VR one. |

## Blocked by a named gap in our own stack

Not compatibility problems — each is a specific missing piece, located with file and line.

| Game | AppID | SteamDB | Notes |
|---|---|---|---|
| fpsVR | [908520](https://steamdb.info/app/908520/) | ✓ | **xrizer does not support OpenVR overlay applications at all**: `Unsupported application type: Overlay` from `xrizer::clientcore`. No overlay app can work — not fpsVR, not OVR Toolkit, none. A missing feature, not a misconfiguration. Its first failure was the launch-options gap (`ERROR_RUNTIME_UNAVAILABLE`); fixing that only revealed the real blocker (2026-08-12, T161). |
| Microsoft Maquette | [967490](https://steamdb.info/app/967490/) | ✓ | Reaches `FOCUSED` with both controllers, then its readiness checklist fails on "controllers on" and "controllers tracking". **The log points at the chaperone, not at tracking**: `GetPlayAreaSize` and `GetPlayAreaRect` unimplemented at `src/chaperone.rs:65` and `:58`. Separately, several right-hand bindings are rejected — `/user/hand/right/input/x/click`, `/y/click`, `/trigger/value` — because X/Y are **left-hand only** on the `oculus/touch_controller` profile (the right hand has A/B), the same class as SUPERHOT's Menu button (2026-08-12, T161). |
| Microsoft Maquette — 2026-08-20/21 T243-night re-test, full 6dof+constellation | | | **Reproduces exactly, unchanged**: wearer confirms the "controllers on" / "controllers tracking" checklist error, same named chaperone gap as before. Not a regression, not affected by tonight's tracking work — the bug lives entirely in xrizer's chaperone stub. |
| Back to Dinosaur Island 2 | [452640](https://steamdb.info/app/452640/) | ✓ | Crashes outright on launch: the user saw CryEngine's "crash handler not found" followed by "bug submission failed" — the reporter failing after the game had already died. The XR session opens and exits in the same millisecond (2026-08-12, T161). |

## Broken — real, reproducible xrizer/Monado bugs

| Game | AppID | SteamDB | Notes |
|---|---|---|---|
| Poly Runner VR | [462910](https://steamdb.info/app/462910/) | ✓ | Reproducible 2/2: gets stuck permanently at OpenXR session state `READY`, spamming `app requested unknown interface "IVRCompositor_013"` in an infinite tight loop (~1300 lines/sec, ~190% CPU), never advancing or exiting on its own — killed by hand both times. Renders normally in flat 2D the whole time (confirmed physically), just never enters stereo VR. `IVRCompositor_013` itself is a confirmed dead end (never existed in any real SteamVR release, see `patches/xrizer/README.md`) — the real bug is why the client-side retry never gives up, still open. |
| Water Bears VR | [394130](https://steamdb.info/app/394130/) | ✓ | xrizer's compositor recreates the swapchain every single frame, for both eyes, forever (`recreating swapchain` spamming in `compositor.rs:1247`, ~65 cycles/sec) — never stabilizes a presentable frame, panel stays dark. Game itself is healthy: input works (trigger gives audible feedback) and it renders fine to its own flat 2D mirror the whole time. Real cause not fully isolated — likely a per-eye texture/bounds mismatch that `is_usable_swapchain()` never accepts, worth a closer look. |
| Water Bears VR — 2026-08-20/21 T243-night re-test, full 6dof+constellation | | | **Reproduces, same shape as before**: wearer "45fps, no image in headset, but the 2D reflects its position" — matches the original panel-stays-dark/2D-mirror-works symptom exactly, still a real, distinct xrizer bug, not the tracking-family issue. Not a regression from tonight's work. Separately noticed in the log during this session: a controller (`device 0`) with a constellation position prior 155.58m from the camera — 19000+ failed seeded-recovery attempts against a 3.0m plausibility bound — a genuine tracking pathology worth a closer look on its own another night, unrelated to this title's actual bug. |
| War Robots VR: The Skirmish | [672640](https://steamdb.info/app/672640/) | ✓ | Calibrates fine, then drops to backlight-only with an un-skippable "put on your VR helmet" prompt. Root cause spans two repos: xrizer never implements HMD presence/worn detection (`ShouldApplicationPause`/`IsInputAvailable` in `src/system.rs` are hardcoded stubs), and Monado's `wmr_hmd.c` already reads the real proximity sensor but never wires it into Monado's own working `XR_EXT_user_presence` support. Scoped, not started — see `patches/xrizer/README.md`. |
| War Robots VR — 2026-08-20/21 T243-night re-test, full 6dof+constellation | | | **Reproduces the known presence-detection bug exactly**: "calibrated and went black" — unchanged, still the named xrizer/Monado presence-wiring gap, not a regression. Also shows the tracking-family symptom on top: "30fps, started spinning, menu in front." Two independent, already-understood bugs stacking on this one title. |
| IL DIVINO - Michelangelo's Sistine Ceiling in VR | [1165850](https://steamdb.info/app/1165850/) | ✓ | Menu renders 2D-only on the desktop; entering the experience gives audio in the headset but **no image** (backlight only). Session reached `FOCUSED` with both controllers registered, launch options confirmed applied — the failure is in the render path, cause not investigated (2026-08-09, one attempt). |
| IL DIVINO — 2026-08-20/21 T243-night re-test, full 6dof+constellation | | | **The old no-image bug did NOT reproduce this time — real progress on that front, but a different failure took over.** Wearer: "started at 45fps, now at 30. See something far away, game continues. Same issue [as the rest tonight]. Here I can't turn much, it re-centers doing strobing." New symptom variant: fps degrading within one session (45→30) and a strobing/re-centering artifact when trying to turn, on top of the usual tracking-family displacement. Worth a dedicated look once the underlying SLAM-load issue has a fix — this title may be genuinely closer to working than the original 2026-08-09 verdict suggested. |
| Meditation VR | [1301850](https://steamdb.info/app/1301850/) | ✓ | All-black in the headset, nothing ever shows. Log-clean: `FOCUSED`, controllers on `oculus/touch_controller`, one harmless `IVRExtendedDisplay_001` unknown-interface probe (same benign category as the `IVRCompositor_013` lesson). Same "session healthy, renders nothing visible" shape as the player's own LoadPhotoTexture trap — not investigated further (2026-08-09, one attempt). |
| Aircar, **on the everyday system specifically** | [1073390](https://steamdb.info/app/1073390/) | ✓ | **Not a contradiction of the ✓ working row above** — that verdict is from the lab machine (GNOME/Wayland/90Hz/patched); this is a fresh xrizer build on the everyday system (KDE/X11/60Hz/unpatched, see `docs/pruebas.jsonl` T152 for the environment note), never tested there before 2026-08-11. Stays in flat 2D through several launch-plumbing fixes (canonical launch options, Steam-wide env export, `-vr` flag) — `PROTON_LOG=1` proved the game's own `openvr_api.dll` DOES load and DOES reach `vrclient_x64.dll` (xrizer's real bridge), cycling load/unload 4 times before the process exits on its own after ~9s. So the OpenVR init handshake is being attempted and failing, not skipped — genuinely different symptom from anything else in this table. Not root-caused; needs narrower `WINEDEBUG` channels or a second already-lab-verified Proton title (SUPERHOT, Propagation VR) tried here to check if this is Aircar-specific (older OpenVR SDK 1.0.16) or a general regression on this machine's xrizer build. |
| Aliens Attack VR, **on the everyday system specifically** | [932190](https://steamdb.info/app/932190/) | ✓ | **Second title reproducing Aircar's exact non-connection signature on this machine** (2026-08-11, `docs/pruebas.jsonl` T154-T155) — installed fresh, canonical launch options set via the Steam UI, game runs fine in flat 2D on the desktop monitor, headset panel powered but blank. Zero new `client_connected` in Monado's log, and `~/.local/state/xrizer/xrizer.txt` doesn't exist anywhere on disk — xrizer never even opens its own log. Confirmed this isn't a plumbing gap specific to this title: the Proton-side bridge (`compatdata/932190/pfx/drive_c/vrclient/bin/vrclient_x64.dll`) and prefix-local `openvrpaths.vrpath` are already correctly wired, reusing the same setup validated for Aircar. A `PROTON_LOG=1` relaunch to compare traces against Aircar's is queued, not yet captured. Two already-lab-verified titles both failing identically here is a real update to the working theory in the Aircar row above: this reads increasingly like a general everyday-system/KDE xrizer regression, not something specific to Aircar's older OpenVR SDK version. External research (not project-specific) found three open, unresolved public bug reports for NVIDIA + Proton + OpenXR (`ValveSoftware/steam-runtime#782`, `ValveSoftware/Proton#7228`, an NVIDIA Developer Forums thread on driver 550.76) — none symptom-for-symptom identical to ours, but real precedent that this stack is independently fragile on NVIDIA. **Standing plan**: if the pending PROTON_LOG trace matches Aircar's pattern, stop chasing this here and resume validation on the dev/lab machine, where the full sweep already works. |

## Failed — unrelated to xrizer/Monado (Proton/engine-specific)

| Game | AppID | SteamDB | Notes |
|---|---|---|---|
| NVIDIA® VR Funhouse | [468700](https://steamdb.info/app/468700/) | ✓ | **Verdict suspect, needs a retest (2026-08-12, T161).** This title's launch options were **missing `PRESSURE_VESSEL_FILESYSTEMS_RW`**, so it could not see Monado's socket from inside pressure-vessel and could not have reached VR regardless of PhysX. The recipe is now complete; the Proton/PhysX/CUDA verdict above was recorded under the broken configuration and should not be trusted until it is run again. |
| InCell VR | [396030](https://steamdb.info/app/396030/) | ✓ | **Root cause corrected 2026-08-12 (T161).** The game is a **native Linux Unity build** that calls Oculus's `OVRPlugin` before anything else (`OVRSwitcher.GetVRActiveDevice`). `libOVRPlugin.so` has never existed on Linux — Oculus dropped the platform in 2015 — so Mono throws `DllNotFoundException` and the process aborts in under a second. **Nothing here ever reached VR**, so the earlier `VR_InitInternal` verdict described a stage the game could not get to. `tools/ovrplugin-stub/` fixes that: a 78-symbol stub answering "no HMD" (install into `InCell_Data/Plugins/x86_64/` — **not** `Mono/x86_64/`, which is only the fallback path the log advertises). With it the stub's trace shows `ovrp_GetStatus` being called and the stack continuing through `SteamVR.CreateInstance` → `OpenVR.Init` → `OpenVRInterop.InitInternal`, where it now dies — so the old verdict was right about WHERE, one stage later than anyone could previously observe. **Next suspect**: the game bundles its own OpenVR 1.0.x-era `libopenvr_api.so` in `Plugins/x86_64/`; an ABI mismatch against xrizer would explain a crash this early (xrizer never opens its log). Not investigated. |
| InMind VR | [343740](https://steamdb.info/app/343740/) | ✓ | **Same root cause as InCell VR, corrected 2026-08-12 (T161)** — same studio (Nival VR), same native Linux Unity build, same `DllNotFoundException: OVRPlugin` (`OVRManager.Update` → `OVRPlugin.get_hasVrFocus`). The "unrelated Mono crash" recorded earlier is the *symptom* of a missing native library, not a cause. **Stub tried 2026-08-13 (T167): it works as designed and moves the failure two stages forward.** With `libOVRPlugin.so` in `InMind_Data/Plugins/x86_64/` the DllNotFoundException is gone (`Player.log` shows `Oculus Utilities v1.3.2, OVRPlugin v0.0.0` — the stub answering) and the game boots and plays music. Fullscreen it dies in seconds with a Mono fatal error under a storm of `XError: BadMatch` (Unity 5.3-era fullscreen vs XWayland); windowed (`-screen-fullscreen 0`, launched directly) it survives past that but spams `NullReferenceException` in `OVRManager.Update` every frame, renders black, and exits on its own after ~20 s of music. Remaining gap: unlike InCell, this build has **no OpenVR plugin in `Plugins/x86_64/`** — its Linux VR path is Oculus-only, so "no HMD" from the stub leaves it nothing to fall through to. Real VR here means the parked LibOVR→OpenVR shim, not another stub tweak. Context that pointed the way: the user recalls both titles running on a DK2, which makes a genuine performance or complexity failure implausible. |
| Surgeon Simulator VR: Meet The Medic | [457420](https://steamdb.info/app/457420/) | ✓ | **Reclassified 2026-08-13 (T167), physically verified** — the T073 "crash-loop" verdict no longer holds: the game runs fine in 2D at 450 fps with an in-game "you need a VR headset" warning. Root cause is a named xrizer gap: the game probes **`IVRSystem_007`** (a real 2014-era OpenVR interface version, unlike the nonexistent `IVRCompositor_013`) four times, xrizer doesn't implement it, so the game concludes there is no VR. One throwaway XR session cycle (`STOPPING→IDLE→EXITING`) then stays 2D. Fixable by implementing the legacy interface in xrizer — a real project, not a tweak. |
| World of Guns: VR | [1111760](https://steamdb.info/app/1111760/) | ✓ | **Retested 2026-08-13 (T167), physically verified — the T073 "never gets past the probe" verdict no longer holds.** xrizer now initializes, creates a real OpenXR session and reaches `SYNCHRONIZED` — then abandons it 17 ms later (`STOPPING→IDLE→EXITING`, the same signature as the Dagon/BtDI/Amoreon class) and keeps running in 2D at 144 fps, **showing its VR UI**: a laser pointer that never moves, no input working. The game is in its VR mode UI with the session dropped underneath it. Binary is already the VR one (`disasm_vr.exe`), so this is not a missing `-vr` flag. |
| Overkill VR | [518720](https://steamdb.info/app/518720/) | ✓ | Confirmed physically (was log-only "inconclusive" in T073): xrizer shows a throwaway session cycle at launch (READY→EXITING in 18ms, same shape as VRChat/Overkill's own harmless probe) then goes silent — no second real session ever opens, even after waiting well past the process's own high-CPU "loading" window. Desktop mirror shows a plain white screen, headset shows nothing. Not the slow-Unity-boot theory T073 floated — it just never gets further. |
| Welcome to Chornobayivka VR | [2064150](https://steamdb.info/app/2064150/) | ✓ | Confirmed physically (was log-only "ambiguous" in T073): reaches `FOCUSED`, real 3D, controllers load fine (`oculus/touch_controller` on both hands) — **Re-verified 2026-08-13 (T167) on the lab 6dof+constellation stack: works, ~50 fps, head tracking good** (controllers still bad that session — the pre-patch-0037 constellation starvation, project-wide, not this title). Original 2026-08-12 findings below still stand: the game's own camera has a fixed **roll** (world tilted sideways), always the same initial angle, present from launch and unaffected by recentering. Our recenter patch doesn't touch it either way, by design (it only ever resets yaw + position, same as real SteamVR) — consistent with the game doing its own one-time "calibrate forward" at startup that captures the player's full incidental head tilt as its reference "level" instead of extracting gravity-aligned yaw only, baking the error in permanently. Also dropped the user somewhere with no visible menu, "lugar raro" (odd spawn location). Normal head pitch/yaw tracking is otherwise correct — this reads as a bug in the game's own camera rig, not an xrizer/Monado issue. |
| Welcome to Chornobayivka VR — 2026-08-20/21 T243-night re-test, full 6dof+constellation, GPU 210W | | | **The pre-patch-0037 constellation starvation this row's predecessor flagged did not get isolated cleanly** — instead the same session reproduced the exact "flying away" SLAM-starvation mechanism just found on Dead Herring VR: wearer (translated) "same as Chornobayivka, I'm flying away", then **30fps** on Steam's counter (=90/3, matching SafeZoneVR's ratio rather than Dead Herring/Google Earth's 45=90/2 — each title apparently settles on its own submultiple, not one shared global cap). Anchor age measured 111-176ms under real CPU load (`monado-service` + `bslt-optflow` threads at 43-60%, lighter than Dead Herring's >80% but still clearly loaded) — same signature, **zero** `det(Q1Jl)==0` divergence events. **This makes the anchor-age/flying-away mechanism 2/2 reproducible across titles the same night**, worth treating as a real, named phenomenon (SLAM position solves falling behind real-time under CPU contention, letting IMU-only extrapolation run away) rather than a per-title curiosity. Not re-tested this session with constellation off to see if the specific starvation this row originally described (2026-08-13) is now actually fixed — that comparison still needs a clean run. **User context on the title itself, not our stack**: on Windows this game has fiddly weapon-grab controls — a dropped weapon is hard to pick back up — and the user's own experience is that it only ever played well on a Quest 2, not clean evidence either way about this rig. Worth separating "hard because the game is hard" from "broken here" before blaming this stack for anything control-related on a future retest. |
| Dark Room VR | [1394640](https://steamdb.info/app/1394640/) | ✓ | Was blocked by a silent first-run dialog exactly as suspected (a hidden "headset may not be supported" warning behind the main Steam window) — cleared once the user clicked through it. After that, reproduced identically twice: xrizer does its usual harmless throwaway session cycle, then goes quiet — headset shows nothing at all, game only ever renders to its 2D desktop window (no VR menu/options visible there either). Inside the game's own 2D window a plain black square covers the play area the whole time (its own failed VR-view placeholder, never an actual crash) — everything else in the window keeps running and responding normally, and it exits cleanly. Same failure shape as Overkill VR: reaches xrizer briefly, never opens a real VR session. |

## Untested / inconclusive

Nothing left here as of this sweep — every T073 backlog title has now been either confirmed
working, confirmed failed, or blocked on something outside this agent's reach (Dark Room
VR's dialog, since resolved).

| Game | AppID | SteamDB | Notes |
|---|---|---|---|
| Arkio | [1053760](https://steamdb.info/app/1053760/) | ✓ | **First-ever attempt, 2026-08-21 (T243-night).** Wearer (translated): "crashed looking for VR when I activated it." Real OpenXR client did connect and `BEGIN_SESSION` happened (`~150ms` real session), compositor frame timing was clean the whole time (gpu 3.3ms median, total 6.3ms, well under budget) and `monado-service` stayed alive with no segfault — reads as an app-side crash on VR init, not something this stack broke. Not root-caused further this session. |
| Playable Alpha Demo | [3494600](https://steamdb.info/app/3494600/) | ✓ | **First-ever attempt, 2026-08-21 (T243-night).** Stuck at what the wearer described (translated) as a ZX Spectrum-style loading screen — "black square in the middle, flickering borders... just like it" — reproduced identically on a genuinely fresh Monado restart, so not session-state related. The title's own game process never registered a named `client_connected` in Monado's log either time — only Proton/Steam's OpenXR-loader self-probes did (`application_name: 'steam'` and `'wineopenxr test instance'`, both transient), and the first of those saw a severe pacer stall right as it connected (**2194 periods jumped forward in one shot**) before disconnecting. Reads as the game's own OpenXR init possibly stalling on that same timing hit, rather than a plain launch-options gap — worth a retest once the `pop_pose()` crash (`docs/06`) or whatever's behind the pacer jumps is better understood, not just with launch options changed. |

## Not a game

| Item | AppID | SteamDB | Notes |
|---|---|---|---|
| SteamVR | [250820](https://steamdb.info/app/250820/) | ✓ | **Installed, and deliberately not used.** This whole project replaces it: xrizer reimplements OpenVR on top of OpenXR precisely because SteamVR's `vrmonitor` does not work here (`docs/06`). Steam also silently re-adds it to `openvrpaths.vrpath` on every startup — there is a trap entry for that below. Nothing to test; if it ever runs, something is misconfigured. |
| Windows Mixed Reality for SteamVR | [719950](https://steamdb.info/app/719950/) | ✓ | **Installed, and not applicable on Linux.** It is the WMR *driver plugin for SteamVR*, i.e. Microsoft's Windows-only path for talking to this headset. Monado's own `drv_wmr` does that job natively here. Listed only so the question "did we ever try it?" has an answer. |
| fpsVR | [908520](https://steamdb.info/app/908520/) | ✓ | A SteamVR performance-overlay tool, not a VR title. First real attempt 2026-08-09: blocked by the launch-options trap below ("VR HMD not found" dialog, `ERROR_RUNTIME_UNAVAILABLE` in xrizer). Worth retrying with UI-set options: xrizer's `overlay.rs` implements `IVROverlay` seriously (1600+ lines, up to `IVROverlay028`), so the prognosis isn't hopeless — still pending. **2026-08-21 user clarification**: not applicable to xrizer/Linux at all — it's a Windows-only overlay that auto-launches and attaches to the left arm for tracking per-title performance, already covered on Windows; not worth re-chasing here (`## Blocked by a named gap in our own stack` above already has the real xrizer-side verdict: no overlay support, period). **Idea parked the same night**: the tool's real value may be separable from its overlay rendering — check whether fpsVR has a log/CSV export mode for its per-frame data independent of the VR HUD, before assuming the overlay gap blocks getting any use out of it at all. |
| OpenVR Benchmark | [955610](https://steamdb.info/app/955610/) | ✓ | **Not in this doc before tonight — found installed and untested, 2026-08-20/21 T243-night.** A standalone GPU stress benchmark (`cyubeVR/Binaries/Win64/OpenVRBenchmark.exe`), not tied to any game's own content — runs through the same OpenXR/xrizer/Monado path as everything else. Full 6dof+constellation stack: **18.3 fps at 3024x3024 (the render-scale-140% resolution used all night)**. No wearer report on positional/tracking symptoms this run (a numeric benchmark score, not a played experience) — worth a repeat once the SLAM-load issue has a fix, as a clean before/after number independent of any title's own engine quirks. |

## Trap: Steam launch options edited on disk don't exist (2026-08-09)

Every VR title here needs the same launch options
(`XR_RUNTIME_JSON=... IPC_IGNORE_VERSION=1 PRESSURE_VESSEL_FILESYSTEMS_RW=... %command%`).
**Editing them into `userdata/<id>/config/localconfig.vdf` while Steam is running does
nothing and is actively dangerous**: the running client only reads that file at startup,
launches the game *without* the env vars (`ERROR_RUNTIME_UNAVAILABLE`, game falls back to
2D — how both Aircar's and fpsVR's first attempts failed, and Google Earth VR's "OpenVR
failed to initialize / InterfaceNotFound"), and then **overwrites the file from memory on
exit**, silently destroying the hand edit. The tell in a live diagnosis: the game's
`/bin/sh -c` line in `ps` has no env-var prefix even though the file on disk shows them.
Always set launch options through the Steam UI (Properties → Launch Options) — applies
immediately and survives.

**Update (2026-08-09, later the same day): there's a simpler alternative that skips this
trap entirely.** Exporting the same 3 variables (`XR_RUNTIME_JSON`, `IPC_IGNORE_VERSION`,
`PRESSURE_VESSEL_FILESYSTEMS_RW`) in the shell **before starting the `steam` client
itself**, instead of per-game Launch Options, works: verified live by reading
`/proc/<pid>/environ` on every process in the chain for a freshly-launched, never-before-
configured title (VersaillesVR) — `reaper` → `pressure-vessel`/`srt-bwrap` → `pv-adverb` →
Proton → the actual Windows `.exe` under Wine all inherited the 3 vars with zero manual
Steam UI steps, and the game reached a real `FOCUSED` OpenXR session with both controllers
on the first try. `pressure-vessel`'s sandbox evidently passes through the ambient
environment of the process that launches it (Steam), not just what's baked into a specific
game's Launch Options. This means every *future* title can skip the manual
Properties-dialog step from now on, as long as Steam itself is started with these three
vars already exported — no per-game setup needed at all.

## Trap: Steam silently re-adds SteamVR to openvrpaths.vrpath on every startup (2026-08-11)

Found setting up xrizer fresh on the everyday system (see `docs/pruebas.jsonl` T152).
Pointing `~/.config/openvr/openvrpaths.vrpath`'s `runtime` array at xrizer's build
directory (per xrizer's own README) works — until the next time the Steam client itself
starts. On every Steam startup, it silently re-adds `.../steamapps/common/SteamVR` to
the front of the `runtime` array (confirmed by diffing the file before/after a restart),
without removing the xrizer entry, but present as a second item — a real risk if OpenVR's
loader logic ever prefers the first listed runtime, or if a game specifically probes for a
`SteamVR` string in the path. **Fix: only edit `openvrpaths.vrpath` after Steam is already
running and stable, never edit it and then restart Steam** — restarting undoes exactly the
part of the edit Steam considers its own. This compounds with a second, separate
Proton-side cache: `steamapps/compatdata/<appid>/pfx/drive_c/vrclient/` (a directory
Proton creates containing `vrclient.dll`/`vrclient_x64.dll`) and a **per-title** copy of
openvrpaths at
`compatdata/<appid>/pfx/drive_c/users/steamuser/AppData/Local/openvr/openvrpaths.vrpath`
— both regenerate from whatever the Linux-side file said at the moment of that specific
game's *last* launch, not necessarily the current file content, so a stale prefix-side
cache can silently keep pointing at an old runtime even after the Linux-side file and
Steam's own env are both correct. If a fresh openvrpaths edit doesn't seem to take effect
for a Proton title specifically, delete both of those prefix-local paths before the next
launch to force a clean regeneration.

## Trap: overlapping VR game launches + background Steam downloads can hang the whole
## desktop (2026-08-09)

Launched a second title (`Emergence`) via `steam steam://rungameid/<id>` before confirming
the first one (`Back to Dinosaur Island`) had actually released the compositor — `pgrep`
showed its process gone, but that doesn't guarantee xrizer/Monado had finished tearing down
its OpenXR session cleanly. At the same time, Steam was still downloading several other
titles in the background (including the multi-GB `SteamVR` + `steamvr_environments`
depots). Net effect, live-observed by the user in the headset: **two/three sessions
visibly stacked/layered on top of each other**, not a clean single game view. Checking
`jack-in-wayland.log` afterward showed 10 `client_connected` vs. only 8
`client_disconnected` — real unclosed sessions, not just a visual glitch. System load hit
**20.84** (normally single digits), swap was in active use, and **the desktop became
unresponsive enough that the user had to force-close Steam by hand**. `monado-service`
itself was found dead afterward (crashed or OOM-killed under the load, not investigated
further) and had to be relaunched from scratch.

**Rule going forward: one VR game launch at a time, full stop.** Before launching the next
title: (1) confirm the previous game's process is completely gone (`pgrep`), (2) confirm
Monado's log shows a matching `client_disconnected` for its `client_connected`, (3) check
`free -h` and `uptime` aren't showing memory/swap pressure or an inflated load average, and
(4) check `~/.steam/steam/logs/content_log.txt` for the client still actively downloading
something — pause/let it finish first. None of this touches the cable/USB physically, but
heavy concurrent load is a real confound for any zero-touch stability test: a USB dropout
seen during a session like this can't cleanly be attributed to the cable degrading at rest,
since system-level resource starvation is a live alternative explanation.
immediately and survives.

## Non-Steam titles

Standalone DK2/DK1-era demos and tech demos that aren't Steam apps at all — no AppID, no
Steam Play sandboxing, just a Windows binary run directly under Wine. Table shape kept
consistent with the sections above (Runtime/Requirements/Status) minus the Steam-specific
columns.

| Item | Source | Status | Notes |
|---|---|---|---|
| Blade Runner 9732 (Deckard's apartment tour) | [developer site](https://br9732.quentinlengele.com/) | Not on Steam | **Correction 2026-08-12 (T163): a copy DOES exist on this machine**, at `~/vr/nonsteam/BR9732/`, with a Proton prefix already set up at `~/vr/nonsteam/compatdata`. The earlier note here said there was nothing to test; that is no longer true. Unity 2017.3.1f1, ships its own `openvr_api.dll`. **Status: runs in 2D, never enters VR.** Launched directly via `proton run` (not through Steam) it starts, and switching to VR — from its own menu or with `-vrmode openvr`, which only skips the 2D intro — leaves the headset on backlight with no image and **no client ever connects to Monado**. Root-caused as far as the library chain: the game's `openvr_api.dll` loads, Proton's `vrclient_x64.so` loads, and **`libxrizer.so` is never loaded** (checked in `/proc/<pid>/maps`). xrizer's own layout is correct — `bin/linux64/vrclient.so` symlinks to it and `openvrpaths.vrpath` points at the right directory — so this is the **launch method**, not the title: every working title here goes through Steam, which sets up pressure-vessel and the Steam client environment that Proton's vrclient shim expects. **Next step: add it as a non-Steam shortcut in the Steam UI** and give it the same launch-option recipe as the rest, then retest. |

If other old standalone binaries turn up (the user recalls having several from the DK2 era),
add them here the same way — they don't need a SteamDB link, just where the binary came from.

## 2026-08-21 T243-night sweep — consolidated verdict, mined from the saved logs, before mass uninstall

20 titles re-tested in one night with full 6dof+constellation, `PROTON_LOG=1`/`WINEDEBUG` full
logging kept for every launch (`~/vr/logs/steam-<appid>.log`, not committed per the media-out
policy). Purpose: not "does each title work" (17-18 came back broken the same way) but "how
many genuinely DIFFERENT problems are actually in this pile", so one exemplar per problem can
stay installed and the rest can go without losing anything.

**API surface (`IVR*_NNN` requests) does NOT distinguish sub-categories of the tracking bug.**
Grepped every saved log for `IVR[A-Za-z]+_[0-9]+`: the titles hit by the SLAM/constellation
starvation span `IVRClientCore_002/003`, `IVRChaperoneSetup_005`, `IVRSystem_012/015/019`,
`IVRExtendedDisplay_001`, `IVRSettings_001` in every combination, with no pattern separating
which ones broke worse. This is real, useful evidence that the bug is orthogonal to which
OpenVR interfaces a title happens to call — it lives underneath all of them, in the tracking
pipeline itself, not in any particular API path.

**One real outlier found by mining exception counts, not by ear**: `grep -ic "unhandled
exception\|backtrace:"` across all 20 logs sits at 9-18 for every title except **Tank Mechanic
Simulator VR (1463010): 405**, almost all `Exception 0xc0000005` (access violation) in a tight
burst around one timestamp. This is a second, genuinely distinct problem on top of the shared
tracking issue — plausibly the real explanation for that session's "hands don't point in the
direction they should" symptom, not just SLAM staleness. Worth its own retest once isolated.

**Final problem categories, one exemplar each:**

| Problem | Exemplar to KEEP | Mechanism | Redundant copies (safe to uninstall) |
|---|---|---|---|
| SLAM/constellation CPU starvation → fps ceiling + runaway extrapolation | **Dead Herring VR** (has the full 3-way tracking-mode A/B on file) | Anchor age spikes to seconds under CPU load (`monado-service` 370-520%), confirmed on a genuinely fresh restart too — not just session accumulation | ISS Tour VR, SafeZoneVR, Chornobayivka VR, Cosmic Flow, Interkosmos, Emergence, SUPERHOT VR, VRChat, VRSailing, Propagation VR, VersaillesVR, Audio Factory, Blast the Past, Google Earth VR, Aliens Attack VR, Reelease |
| Same tracking bug + a second, distinct internal crash storm | **Tank Mechanic Simulator VR** | 405 `0xc0000005` exceptions in one burst, ~30x every other title's baseline | — |
| xrizer swapchain never stabilizes (unrelated to tracking) | **Water Bears VR** | `recreating swapchain` loop, `compositor.rs:1247`, pre-existing bug | — |
| xrizer has no HMD presence/worn detection (unrelated to tracking) | **War Robots VR** | `wmr_hmd.c` reads the real sensor, never wired to `XR_EXT_user_presence` | — |
| xrizer chaperone stub (`GetPlayAreaSize`/`Rect` unimplemented) | **Microsoft Maquette** | Only title in the whole catalog that needs chaperone for its own readiness check | — |
| xrizer has zero OpenVR overlay support | *(not applicable here — Windows-only tool, per user)* | `fpsVR`'s `overlay.rs` gap, already scoped | fpsVR |
| GPU/CPU stress baseline, not tied to any game content | **OpenVR Benchmark** | 18.3fps @ 3024x3024 tonight — keep for a clean before/after once the tracking bug has a fix | — |
| Reference title, controller-input inconclusive this session | **Aircar** | No Xbox pad connected during the retry; not counted as broken | — |

That's **8 real, distinct problems** behind the night's 20 broken-looking titles, not 20 separate
bugs. Full per-title notes (fps, wearer quotes, anchor age, pacing) stay in the tables above —

**Open hypothesis, not yet checked (user's own idea, 2026-08-21)**: the fps ceiling isn't
one fixed number — 30, 40, and 45 fps have all shown up on different titles the same night
(Aperture Hand Lab at 30, Propagation VR at 40, Google Earth VR/Dead Herring VR at 45).
Worth checking whether this correlates with **engine age/generation** — older engines that
were never built with variable/high refresh rate in mind may internally target a fixed
60fps (or lower) and simply can't be pushed past it regardless of what the compositor
offers, while more modern engines negotiate the real rate. If true, this would be a genuine
per-title ceiling baked into each engine, not (or not only) the shared SLAM/CPU-contention
story above. **Next step, not done yet**: check whether any of these titles expose a launch
flag or config file forcing a target frame rate, and look for a correlation between each
title's engine/release year and its observed fps ceiling across the table above.

**RESOLVED 2026-08-21, T244 — the fps ceiling was the APP PACER, not the GPU, not the tracking
stack, not the engines.** Measured with the real instrument (count of `Delivered frame` lines
with `U_PACING_APP_LOG=debug`, docs/32), Dead Herring VR, fresh `monado-service` each run:

| Run | Config | app fps | GPU (nvidia-smi) |
|---|---|---|---|
| A | as the night before (pipelined pacer, render scale 140%) | **44** | 168 W / 54% |
| B | `XRT_COMPOSITOR_SCALE_PERCENTAGE=100` (half the pixels) | **45** | 90 W / 39% — **not GPU-bound** |
| C | serial pacer (`VR_PIPELINED=0`) | 63 | — |
| D | pipelined + patch 0092 (promise/gate bookkeeping) | 58 | — |
| E | D + `U_PACING_APP_USE_MIN_FRAME_PERIOD=true` | **89-90** | 161 W / 54% |

Mechanism, two layers: (1) the pacer's `gpu` column is structurally ~1 display period (the
multi-compositor's wait thread only starts waiting on frame N's fence after frame N-1 was
consumed, so `gpu_done` is quantised to the compositor tick — docs/32 had already caught it
not moving with pixel count). `calc_app_period()` doubles the app period whenever that column
exceeds 11.11 ms, and it flaps on that edge: 37-42% of frames at `period: 22.22` → 58-63 fps.
(2) the pipelined model (T175) required the next promise to lie past the previous *shifted*
promise and then added the shift again → promises two periods apart by construction → a hard
45 fps whenever pipelined engaged, which with the bogus gpu column was always. A title whose
`draw` also tripped the doubling landed on 30 (=90/3): the per-title 30/40/45 submultiples are
the same bug through different stages, not engine age. **Fix shipped**: patch 0092 + the
launcher now sets `U_PACING_APP_USE_MIN_FRAME_PERIOD=true` by default (`VR_MIN_PERIOD=0`
reverts). Wearer on Dead Herring VR at 90 fps (translated): "smooth, correct position...
overall good". The engine-age hypothesis above is closed — not the cause. Every title in the
"SLAM/constellation CPU starvation → fps ceiling" row needs a retest under the new default;
the "flying away" half of that row was a separate bug, also fixed the same session (docs/06,
"The wearer gets relocated on every companion drop").

**First retests under the new defaults, same day (T244, 0092-0094 + `U_PACING_APP_USE_MIN_FRAME_PERIOD`, render scale 100%):**

| Title | before (T243) | after (T244) | wearer (translated) | hands |
|---|---|---|---|---|
| Vertical Shift (1807480) | 30 fps, "far from the map" | **66-77 fps** (`Delivered frame` count), 87 W / 42% GPU | "well positioned, no headset drift" | never visible in this title in any session — inconclusive for controllers, not counted against it |
| Sniper Elite VR (752480) | 45 fps "at moments", "world very high" | **89 fps**, GPU not limiting | "rest fine"; a blue pointer from a fixed point in front that does not move, game waits for a recalibrate-controls OK | the run was with `WMR_CONSTELLATION_CONTROLLERS=0` (the launcher's `6dof` default — constellation is only implied by `ctrl`; export it explicitly) → controllers had orientation but no position, which this title refuses. Retest with constellation ON was blocked by the USB2 branch dying outright at 17:15 (`device not accepting address, error -71`, census 2/5 SS-only, T186 cold replug needed). |

| Sniper Elite VR (752480), second run, constellation ON, both controllers registered at start | — | **87 fps** | "same, no controllers; I'm higher than I should be but that's a known thing of this title — that's why it starts with the calibration; pointer immobile, but good performance at least" | **the driver IS tracking them**: `constellation sample #1440 … matched_blobs=6 reproj_err_px=0.17`, solve-yaw correction `locked=true` on the right. Monado has the poses; the immobile pointer is an **xrizer ↔ title input/pose-binding problem** — a new, separate class (the title also waits for a "recalibrate controls" confirmation that never arrives, so its action bindings probably do not resolve). Not investigated this session; needs a PROTON_LOG/xrizer-log run. |

| Dead Herring VR (1498490), constellation ON, both controllers registered at start, 11 companion drops during the run | 44-45 fps + "flying away" | **80-90 fps**, no relocation | "left controller visible, usually anchored but rotating well; then it jumps in front of me, where it belongs — roughly 70% wrong / 30% right; the right one does not appear, quite possibly the game itself, same happened in Google Earth" | `constellation sample #2130 matched_blobs=12`. This is docs/58's known residual (parked-at-anchor, late jumps toward the real position; worn presence ~50-60%), unchanged by today's fixes and not their target. The fps ceiling and the relocation are gone on this exemplar. |

| Wolfenstein: Cyberpilot (1056970), constellation ON, both controllers registered at start — **CAVEAT: Dead Herring VR was still running in the background the whole run** (its Wine tree survived a wrapper-only kill; Monado client 7 stayed connected until teardown; `scripts/game-stop.py` now exists because of this) | 30 fps, "very promising" | **70-80 fps by the wearer's read, WITH a second title rendering behind it** — so a lower bound; the instrument's 151/s and the 675% CPU / 183 W were two clients, not this title | "70 fps. A bit hard because of the bad stick sensitivity; the headset motion still having that constant delay — or rather the vertical readjustment — is nauseating. But I managed to start playing and it's a good example overall" — and at close: "at moments up to 80 fps, very solid. The controllers here are the definitive test: very complex to get right, but worth it. This is one of the big games and worth making work" | Controllers auto-slept during the ~100 s load (docs/03 auto-sleep) and re-attached on power-on within the same session. Two residuals named by the wearer, neither new: stick sensitivity (`WMR_STICK_DEADZONE=0.15` is the launcher default — a per-title tuning candidate) and the head-pose latency/vertical settle (SLAM anchor age ~150 ms under load + docs/58). |

Two launcher facts worth knowing for the rest of the retest list: (1) `up 1 6dof` does NOT turn
controller constellation on — T243's runs exported `WMR_CONSTELLATION_CONTROLLERS=1` by hand;
(2) a controller powered on after `monado-service` started gets its config read (battery shows
up) but stays `<none>` in the role list — Vertical Shift never showed it; a clean restart with
both powered on registered `left:`/`right:` at once. Start the controllers BEFORE Monado.

**Cutoff decided 2026-08-21, late-night follow-up**: the physical panel itself confirmed
staying at true 90Hz all night (`found display mode 4320x2160@90.00` in every launch's log,
never fell back to 60Hz) — so the "flicker" feeling on low-fps titles is compositor
frame-repeat/reprojection judder from a low app render rate, not a regression of the
project's original 60Hz-backlight-strobe fix. Given that, **titles stuck at a low app render
rate (the broken family throughout this section) are being deprioritized from further live
testing rounds** until the underlying cause (`docs/06`'s `pop_pose()` crash, and/or whatever
else is capping the render rate) has an actual fix — retesting the same symptom on more
titles without a fix teaches nothing new.

this section is the compression layer for the uninstall decision, not a replacement for them.

## Reference

Full session-by-session detail for every entry above is in `docs/pruebas.jsonl` — search by
game name or by the T-numbers cited per row (T063-T086 cover this sweep as of
2026-08-08).

## Sweep methodology & storage tiers (user-directed, 2026-08-18)

The catalog is a ROTATING pipeline with a disk budget, not a static install base:

1. **Small titles (the majority)**: download in batches → launch-test → verdict lands
   in this file → **discard unless working/showcase-worthy**. Storage is rented, the
   verdict is what we keep.
2. **Retest triggers**: every stack fix that kills a named failure class triggers
   re-downloading exactly the titles that died of it. Current queue:
   | fix (landed) | retest |
   |---|---|
   | Presence end-to-end (monado 0075 + xrizer 0003, needs first don/doff calibration) | War Robots VR: The Skirmish (672640) |
   | Legacy input resurrection (xrizer 0004) | Blade Runner 9732 (non-Steam); any legacy-API title |
   | Menu pass-through (xrizer 0004) | SUPERHOT VR menu button (installed) |
   | Launch options completed | NVIDIA VR Funhouse (468700) — its FAIL verdict was measured broken |
   | 10-second Steam-UI toggle (force Proton) | BlazeRush (302710) |
   | Chaperone GetPlayAreaSize/Rect (small fix, not yet built) | Microsoft Maquette (967490) |
   | IVRSystem_007 legacy interface (real project, not built) | Surgeon Simulator VR (457420) |
3. **Big titles (Alyx class)**: live on a separate/permanent tier (mechanical disk
   candidate), prewarmed to RAM via `scripts/vr-prewarm.sh` at pick time (cache mode
   works from mechanical). Half-Life: Alyx specifically is the ultimate showcase exam
   for this stack — native OpenVR, heavyweight — plan its tier before downloading ~70G.
4. **Prewarm discipline**: demo titles get `vr-prewarm.sh` before a session (page-cache
   or tmpfs mode) — zero frames lost to storage, same philosophy as the CSV ramdisk.

### Next sweep batch (user-picked candidates, 2026-08-18)

| candidate | size note | tier thought |
|---|---|---|
| CyberPilot VR | ~15G — "entra apenas" | fits now: 2026-08-25 RAM upgrade to 32G, tmpfs bumped 10G→20G and vr-prewarm.sh's ram-mode cap 12G→16G — not yet actually run through `--mode ram`, just eligible |
| Tank Mechanic Simulator VR (1463010) | already a ✓ row (2026-08-09, 3dof era) | "tema serio ahí" — re-verdict on the current full-6dof stack |
| Star Wars: Droid Repair Bay | small (the big Star Wars titles don't fit the budget) | free title, showcase-friendly |
| Sniper Elite VR | fits | fresh test |
| OpenVR Benchmark (955610) | tiny, FREE | ⭐ deterministic 60s score, per GPU+headset leaderboard -- THE "better than Windows" head-to-head metric (dual-boot same machine) + full OpenVR-path exercise |

Rule of thumb settled with the budget: the download-test-discard loop targets titles
that fit the 10-15G working budget; anything bigger waits for the permanent/mechanical
tier decision (the Alyx class).

| OpenVR Benchmark | [955610](https://steamdb.info/app/955610/) | ✓ | **Runs perfectly through the stack (2026-08-18, T217) — but its score is runtime-capped, NOT a GPU measurement here**: identical 111.11 avg/0.1%/0.3% at BOTH 3024² and 2160² render targets = the frame loop is granted xrWaitFrame every exactly 9.00ms by Monado's app pacer (111 fps rendered, 90 displayed, ~19% discarded — a named investigation, see NEXT-STEP). Useful as a deterministic loop-pacing instrument and as the future beat-Windows head-to-head ONCE the pacer question is settled. Its supersampling "fix" buttons spawn an Overlay helper — xrizer's no-overlay gap (fpsVR class), second motivator. `XRT_COMPOSITOR_SCALE_PERCENTAGE=100` is the universal-resolution knob (2160×2160 confirmed in its Info panel). First title ever run FROM RAM via vr-prewarm.sh ram mode. **WINDOWS REFERENCE, 2026-08-19 (T226), SAME G2 on the SAME machine (SSD swapped), Oasis+SteamVR 2.16.7, 3060 Ti**: pass 1 = **26.02 avg / 20.39 (1%) / 19.70 (0.3%)** at 2576×2520@90; pass 2 in the same process = **354.77 FPS**, which at 6.49 Mpx on a 3060 Ti is not physical — **the second-run-in-one-process path is broken in the benchmark itself, on Windows too**; on Linux it wedges instead. **Standing rule: ONE run per process lifetime, restart between measurements.** ⚠️ **This app MISLABELS the headset on the Oasis path** — its Info panel reported "Oculus - Oculus Quest2" for a G2 (the render target it prints is SteamVR's G2 target), so the per-GPU+headset leaderboard would file our result under the wrong hardware: verify the label before using it as the beat-Windows metric. **The head-to-head is NOT yet valid**: our 19.25 avg / 9.80 low was at 2160² AND was itself a second-pass number (4.66 warm-up before it). Valid pair = pass 1 vs pass 1, one run per process, same render target — cheap to get by re-running Linux at 2576×2520 via `XRT_COMPOSITOR_SCALE_PERCENTAGE` and taking the first run only. |

### Per-title "kit config" direction (user-named, 2026-08-18)

Every verdict row should eventually carry its perfect triple: (1) exact launch
options, (2) recommended controller mapping for THIS kit (G2 sticks/buttons —
custom binding JSONs versioned in this repo, consumed via xrizer's action-manifest
machinery; no paid remappers needed), (3) VR resource profile (constellation,
threads — vr-launcher.py's TITLE_PROFILES). Overlay-class tools (OVR Advanced
Settings — free+FOSS, fpsVR, the benchmark's settings helper) all wait on ONE
xrizer gap: overlay application support — three motivators and counting.
