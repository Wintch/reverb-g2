nv-debug-*/
nv-report-*/
windows-kit2/
nv-report-*/build/
scripts/.private-patterns
*.[Pp][Dd][Ff]
__pycache__/
*.7z

# Personal/ephemeral files that land loose in the working tree (phone photos dropped for a
# quick look, ad-hoc screenshots, raw webcam capture frames) - never meant for a public repo.
# The repo went public 2026-08-06; before that these were just untracked and harmless, now
# an accidental `git add -A` would actually publish them. Pattern-based, not exact filenames,
# so the next one of these (different timestamp/ID) is caught automatically.
photo_*.jpg
photo_*.jpeg
docs/Screenshot_*.png
Screenshot_*.png
experiments/webcam/

# Capturas de calibracion (fotos/videos de los LEDs, del casco, del banco).
# NUNCA van al repo: es publico y una foto del lab puede llevar algo personal en el encuadre
# sin que nadie lo note al subirla. Los NUMEROS viven en docs/ y en docs/pruebas.jsonl, que es
# lo unico reproducible; el archivo original se queda fuera del arbol (T230).
docs/led-photometry/
*.mp4
*.mov
*.heic
